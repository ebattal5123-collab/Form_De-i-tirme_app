services:
  - type: web
    name: belge-donusturucu
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn main:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
      - key: gizli-anahtar-2026
        generateValue: true
    plan: free
